{
  "name": "ship",
  "version": "0.0.0",
  "private": true,
  "description": "Ship monorepo - API + Web + Shared types",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "dev:api": "pnpm --filter @ship-dhairya/api dev",
    "dev:web": "pnpm --filter @ship-dhairya/web dev",
    "dev:shared": "pnpm --filter @ship-dhairya/shared dev",
    "build": "pnpm --recursive run build",
    "build:shared": "pnpm --filter @ship-dhairya/shared build",
    "build:api": "pnpm run build:shared && pnpm --filter @ship-dhairya/api build",
    "build:web": "pnpm run build:shared && pnpm --filter @ship-dhairya/web build",
    "type-check": "pnpm --recursive run type-check",
    "lint": "pnpm --recursive run lint",
    "clean": "pnpm --recursive run clean && rm -rf node_modules",
    "worktree:init": "./scripts/worktree-init.sh",
    "worktree:status": "./scripts/check-ports.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "prettier": "^3.4.2"
  }
}
